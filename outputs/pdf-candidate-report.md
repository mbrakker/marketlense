# Speedup Proof

## ⚠️ INCONCLUSIVE — Evidence did not clear the proof threshold

> The observed difference does not exceed the noise threshold.

**Project:** MarketLense  
**Target:** Real visual-candidate extraction on the committed three-PDF golden corpus  
**Preserved behavior:** Source PDF hashes, candidate counts, ordered candidate signatures, degraded-page counts, and estimated cost remain identical.  
**Generated:** 2026-08-11

## Before vs after

| Measurement | Before | After |
|---|---:|---:|
| Median runtime | 20,166 ms | 19,650 ms |
| p95 runtime | 21,963 ms | 20,715 ms |
| Mean runtime | 20,200.867 ms | 19,240.4 ms |
| Range | 19,228–21,963 ms | 17,628–20,715 ms |
| Variability | 3.43% CV | 6.01% CV |
| Measured runs | 15 | 15 |

**Speedup ratio:** 1.026×  
**Proof threshold:** 12.02%  
**Correctness:** PASSED

## What changed

Added a real-PDF worker matrix; no production worker setting changed because the leading candidate did not clear the measurement-noise threshold.

**Why behavior remains equivalent:** Every profile executes the same collect_candidates path against the same three golden PDFs with isolated output directories; only the worker count varies.

| Complexity | Before | After |
|---|---:|---:|
| Estimate | Bounded PDF page-chunk scheduling | Bounded PDF page-chunk scheduling |

### Changed files

- [benchmark_pdf_candidate_parallelism.py](../scripts/quality/benchmark_pdf_candidate_parallelism.py) — Real PDF worker-count matrix.
- [test_pdf_candidate_parallelism_benchmark.py](../tests/test_pdf_candidate_parallelism_benchmark.py) — Selection regression tests.
- [benchmarks.md](../docs/quality/benchmarks.md) — PDF worker benchmark method.

## Correctness checks

| Check | Status | Evidence |
|---|---|---|
| PDF candidate benchmark and worker tests | ✅ passed | 7 passed |
| Full matrix outcomes | ✅ passed | All eight worker profiles retained all three source hashes, counts, candidate signatures, degraded-page counts, and zero estimated cost. |

## Benchmark protocol

**Workload:** Three committed golden PDFs; workers one through eight; two warmups and seven measured samples per profile; 15 samples for the targeted 4-vs-5 confirmation.  
**Command:** `python scripts/quality/benchmark_pdf_candidate_parallelism.py --output-root out/pdf_candidate_worker_matrix --output-json out/pdf_candidate_worker_matrix.json --warmups 2 --runs 7`  
**Warmups:** 4  
**Measured runs:** 15 per version

## Reproduce

```bash
python scripts/quality/benchmark_pdf_candidate_parallelism.py --output-root out/pdf_candidate_worker_matrix --output-json out/pdf_candidate_worker_matrix.json --warmups 2 --runs 7
```

## Limitations

- The corpus covers three dense PDFs and visual-candidate extraction only; it does not represent every PDF class or downstream crop/OCR behavior.

## Residual risks

- Other PDF worker pools require independent benchmarks.

## Notes

- Worker four had the fastest initial median, but the 15-sample confirmation reduced the improvement to 2.56%, below the 12.02% noise threshold; the production five-worker baseline remains unchanged.

---

Generated by `$speedup-proof`. Runtime evidence and correctness checks determine the verdict.
