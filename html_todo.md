# HTML TODO

Last reviewed: 2026-02-13

## Remaining items

1. Finalize image SEO width/height metadata.
   - Keep current descriptive `alt` and lazy loading, and add explicit `width`/`height` attributes when image dimensions are available.
2. Improve Core Web Vitals further.
   - Reduce inline CSS bloat (move to shared stylesheet if publish path allows caching).
   - Add stronger image optimization strategy (`srcset`/responsive sizes/compression pipeline hookup).
