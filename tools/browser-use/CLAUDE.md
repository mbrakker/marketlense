# Vendored Browser Use Claude Notes

This file is a Market-Lense-first wrapper for the vendored Browser Use subtree.

If a tool or agent reads `tools/browser-use/CLAUDE.md`, treat the root Market Lense `AGENTS.md` and root repository conventions as authoritative. The upstream Browser Use Claude-specific guidance is preserved only as reference in `UPSTREAM_CLAUDE.md`.

Do not treat the vendored Browser Use subtree as an independent primary repository when working from this workspace.
