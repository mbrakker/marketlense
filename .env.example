# Local secrets. Copy to .env and provide only the values needed by your workflow.
OPENAI_API_KEY=
OPENROUTER_API_KEY=
OPENROUTER_HTTP_REFERER=
# Optional absolute or repository-relative path to an untracked, authorized
# browser form-identity profile. The committed default is deliberately blank.
BROWSER_DOWNLOAD_IDENTITY_CONFIG_PATH=
GOOGLE_OAUTH_CLIENT_JSON=
GOOGLE_OAUTH_TOKEN_JSON=
GOOGLE_SERVICE_ACCOUNT_JSON=
GMAIL_OAUTH_CLIENT_PATH=
GMAIL_OAUTH_TOKEN_PATH=
IMAP_PASS=
WP_SITE_URL=
WP_USERNAME=
WP_APP_PASSWORD=
WP_BEARER_TOKEN=
