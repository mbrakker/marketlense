text: |
  Summarize the report scope using file_search:
  - primary subject/domain
  - geographic coverage (if stated)
  - time horizon (if stated)
  Output only a JSON object with one top-level key: "scope".
